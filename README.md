# UniquePasswordPwned
A small application to check a directory containing plain password text files using HIBP "Pwned Passwords" API.

## Usage
`dotnet run "X:\Documents\Passwords"` *- substitute the directory for your own*
